<div class="container mt-3">
  <h1>All our Flats (<%= @flats.size%>)</h1>
  <div class="row" id="flats">
    <% @flats.each do |flat| %>
      <div class="col-4 mb-5">
        <%= render flat %>
      </div>
    <% end %>
  </div>
</div>
